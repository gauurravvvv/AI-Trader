{
  "name": "aegis",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "dev": "node --env-file-if-exists=.env --import tsx apps/daemon/src/main.ts",
    "smoke:news": "node --env-file-if-exists=.env --import tsx apps/daemon/src/smoke-news.ts",
    "report": "node --env-file-if-exists=.env --import tsx apps/daemon/src/report.ts",
    "smoke:cost": "node --env-file-if-exists=.env --import tsx apps/daemon/src/smoke-cost.ts",
    "soak": "scripts/soak.sh",
    "smoke:debate": "node --env-file-if-exists=.env --import tsx apps/daemon/src/smoke-debate.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@types/nodemailer": "^8.0.1",
    "nodemailer": "^10.0.0"
  }
}
